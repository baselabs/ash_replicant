# AshReplicant — Production-Readiness Roadmap

This is the canonical definition of the work required for the stable major
release. It contains authored scope, acceptance, dependencies, and governing
decision ownership only. Current status and evidence are derived from `.forge/`
machine state:

```bash
python3 ~/.claude/scripts/forge-roadmap.py \
  --repo /Users/rp/Developer/Base/ash_replicant --report
```

No row is optional for the release gate. Enhancements are included where the live
Replicant contract or Ash architecture already supports the capability; they are
not silently narrowed out of the adapter.

## Implementation and verification cadence

This cadence is binding for every roadmap row and every implementation plan derived
from it:

- Non-closeout tasks run only the tests that directly exercise the changed behavior,
  touched-file formatting, and compile with warnings as errors. They do not run the
  repository-wide test suite, Credo, Dialyzer, docs, dependency audits, package builds,
  the complete integration matrix, or GitHub Actions.
- Each row has one explicit closeout task. That task runs the row-complete affected
  test selection and the quality checks required by the row; it does not repeatedly
  rerun unrelated release gates after each fix.
- The complete repository release battery and exact-head GitHub CI run once in E2,
  after all implementation rows and documentation are complete. Any final fix
  invalidates only the directly affected focused evidence until the consolidated E2
  closure battery is rerun.
- Commits remain local during task implementation. Pushes are reserved for a planned
  row closeout or an explicit user instruction, preventing per-task GitHub Actions.

Every implementation plan must label every task with exactly one verification class
and list the exact commands permitted for that task. A task without this label is not
ready to execute:

| Verification class | When used | Permitted | Prohibited |
|---|---|---|---|
| `FOCUSED` | Every implementation task before its row's final task | Named tests that directly exercise the changed behavior, one red-capable mutation where required, touched-file format, compile-WAE | Row-wide or repository-wide suites; Credo; Dialyzer; docs; dependency/security audits; package builds; complete integration matrices; push; GitHub Actions |
| `ROW CLOSEOUT` | Exactly one final task per roadmap row | One row-complete affected test selection and the quality/package/document checks named by that row | Repository-wide release battery; repeated full reruns after each correction; push or GitHub Actions unless the plan or user explicitly authorizes them |
| `RELEASE CLOSEOUT` | E2 only | The complete repository release battery, release-byte checks, and one exact-head GitHub CI run | Per-task or per-row repetition of the release battery |

When a focused or row-closeout check finds a defect, fix it and rerun the directly
affected check. Do not promote that correction into a broader suite. The next broader
gate runs only at its already-declared closeout boundary.

<!-- forge-roadmap-schema: 1 -->

| ID | What | Acceptance | Depends | Why |
|---|---|---|---|---|
| P0 | **Production-readiness program authority** — keep the verified architecture, complete implementation inventory, and machine-derived tracker as the governing release program. slug:production-readiness-program | The T2 program design is independently reviewed and reconciled; every observed gap and recommended major capability maps to exactly one implementation row; strict roadmap parsing passes; future scope changes update the governing design and dependency graph before implementation | — | Program-level design record; implementation remains in A1–E2 |
| A1 | **Restore trustworthy gates** — separate pure/introspection tests from database cases; make local no-database, live integration, and remote CI paths independently non-vacuous. slug:a1-gate-integrity | `mix test --exclude integration` passes with no Repo; integration setup fails closed when configured but unreachable; CI reaches and passes format, compile-WAE, Credo, dependency audit, migrations, all live integration tests, Dialyzer, docs, package, and migration-drift gates; each new/changed gate has a red-capable probe | A2 | New ADR required in this slice: verification and release evidence |
| A2 | **Upgrade and secure the 1.0.0 runtime/dependency floor** — move AshReplicant to current Elixir and adopt AshOnetime while resolving every current Ash, Postgrex, and ymlr advisory. slug:a2-secure-dependency-floor | Package metadata and `.tool-versions` use Elixir 1.20.3/OTP 29; Ash requirement is `>= 3.31.3 and < 4.0.0-0`, excluding vulnerable Ash 3.31.2 and Ash 4 prereleases; the release-candidate lock resolves current Ash 3.31.3, and CI tests exact floor 3.31.3 plus floating latest Ash 3.x with explicit resolution assertions; AshPostgres 2.11, EctoSQL 3.14, a Postgrex release fixing CVE-2026-66838, current safe ymlr path, and AshOnetime 0.6.0 resolve together; `mix hex.audit` and `mix deps.audit` pass; confidential message digest/structural-response behavior is implemented in C1 using AshOnetime's public DSL | — | New ADR required in this slice: 1.0.0 supported runtime and dependency contract |
| A3 | **Coordinate the Replicant major contract** — consume a published Replicant release containing the frozen actual-session identity, batch, message, and incremental-snapshot contracts plus its secure Postgrex floor; release the two packages without orphaning either dependency range. slug:a3-replicant-coordination | The fetched Hex package reports system/database identity from the actual replication session; every callback/type used exists in that package, not only a sibling checkout; a compatibility guard blocks incremental snapshot delivery until C3's provenance contract is present while v1 snapshot compatibility remains live-tested; Replicant's coordination row is closed; a clean resolver proves compatible installs; cross-repo release order and rollback are documented | A2 | Replicant roadmap D2 plus a new AshReplicant dependency ADR |
| A4 | **Reconcile product and release authority** — correct stale version/install/policy/snapshot/tenancy prose, false `v0.3.3` tag link, old action-oriented handoffs, missing lifecycle accounting, and the contradicted Charter B1 mechanism. slug:a4-authority-reconciliation | README, Charter, AGENTS, usage rules, CONTRIBUTING, notebook, CHANGELOG, ADR index, source moduledocs, handoff banners, GitHub releases, Hex release history, and tags agree with live code; 0.3.3 is forensically tied to a commit or explicitly documented without inventing a tag; required foundational ADRs are tracked; grep finds no superseded policy/version/AshOnetime claims | A1 | Existing ADR-0001 plus new ADRs authored by the owning decisions |
| B1 | **Prove one destination transaction boundary** — require every mapped resource, selected action, checkpoint resource, declared auxiliary action, and AshOnetime store to use the same AshPostgres destination Repo; reject cross-Repo and non-Postgres targets before delivery. slug:b1-destination-atomicity | Compile/start admission recursively enumerates current mapped/checkpoint actions plus framework and declared participants; raw/effective Repo, data layer, `touches_resources`, lifecycle generation, and AshOnetime profile mismatches fail before effects; streaming transaction, SCD2, v1 snapshot-batch, and WAL-safe local auxiliary fault/replay tests use append-only observers to prove atomic rollback and distinguish physical repeats from final-state convergence; no batch/message/incremental/append callback is claimed early, and C1–C4 must compose each future capability with this boundary | A2, A3 | [ADR-0006](adr/0006-destination-transaction-boundary.md) |
| B2 | **Bind and serialize checkpoint state** — replace slot-only identity with actual-session source system/database/slot identity plus a canonical publication/resolver contract manifest and digest; lock/update monotonically and define safe migration/reset behavior. slug:b2-source-bound-checkpoint | Replicant's actual replication-session identity binds the row; separate-connection identity is rejected as TOCTOU; lower/equal LSN cannot regress or reapply; concurrent callers produce one effect; compatible manifest transitions replace manifest/digest atomically without changing LSN; incompatible changes halt; existing slot-only rows migrate only with unambiguous evidence or explicit operator choice; snapshot/message frontiers share the identity | B1 | Superseding effect-once ADR required; preserves AGENTS Critical Rule 6 |
| B3 | **Make publication and mapping coverage explicit** — preflight every publication relation and column against the resolver, target attributes, `skip`, and explicit ignores; never advance past an accidental unmapped source. slug:b3-strict-source-coverage | Missing expected tables, duplicate mappings, unignored publication tables, unmapped columns, invalid source types, and changed relation contracts halt before checkpoint advance; explicit qualified table/column ignores are compile/start validated; live additive-column and publication-change tests prove the guard goes red | B2 | New ADR required in this slice: strict mapping and schema compatibility |
| B4 | **Close tenant reassignment and old-record fail-open paths** — make missing or raising old-tenant resolution a structural halt for every relocate/delete/close path and align SCD1/SCD2 behavior. slug:b4-tenant-reassignment | Attribute and MFA tenancy tests cover same-tenant, reassignment, missing old tenant, false/blank tenant, raising resolver, PK/business-key changes, delete, truncate, attribute/context/global strategies; every ambiguous case halts before writes; operator preflight enforces RIF where the live operation requires it; all current docs state the same rule | B1, B3 | [ADR-0001](adr/0001-fail-closed-multitenancy.md) amendment |
| B5 | **Harden classified-data, telemetry, and SQL boundaries** — preserve the ratified AshCloak/binary-storage/skip type-shape contract; make message idempotency confidential; type telemetry values; remove production test hooks and unsafe identifier interpolation. slug:b5-classified-boundaries | Sensitive fields require AshCloak, binary-storage-typed application encryption, or skip; message claims persist only versioned host-keyed digests and fixed structural responses; HMAC rotation retains old key versions through claim/recovery lifetime; error/raise/throw/exit and telemetry mutation tests leak no values; identifier quoting tests cover embedded quotes/control cases; `apply_ledger` is absent from production API/code; secret/package scans pass | B1 | Critical Rule 3 plus a new classified message/value-free ADR |
| B6 | **Freeze host action semantics** — verify selected create/read/destroy/close/append/message actions, transaction participation, tenancy mode, validation/change/encryption execution, authorization bypass, and notifier suppression symmetrically. slug:b6-host-action-contract | Compile diagnostics name every missing/invalid action; `authorize?: false` is explicit and docs never claim policies re-run; changes/validations/AshCloak/tenancy execute; single/bulk/SCD2/append/message notification tests prove the declared no-notifier outcome; action behavior changes have old-contract grep and red gates | B1, B4, B5 | New ADR required in this slice: trusted system action execution |
| B7 | **Secure internal resources and pipeline lifecycle** — make checkpoint/progress resources deny external actors by default; add an adapter `PipelineOwner` that owns each resolver generation and monitors its Replicant pipeline. slug:b7-internal-resource-lifecycle | Generated internal resources include policy authorization and default-deny behavior; sink bypass remains functional; `child_spec/1` starts the adapter owner under a host supervisor; Replicant retains transport ownership; invalid config, duplicate slot, failed connect, independent pipeline halt/crash, owner crash, and normal stop leave no stale/foreign generation; duplicate starts cannot overwrite live configuration | B2, B6 | New ADR required in this slice: internal trust and lifecycle ownership |
| C1 | **Implement transactional and idempotent message actions** — route transactional messages inside their transaction; route non-transactional messages through verified AshOnetime idempotency using source+slot+LSN and a versioned host-keyed content digest. slug:c1-message-actions | Prefix routes and explicit ignores are validated; unknown prefix halts; transactional order/ordinal, rollback, replay, and batch composition pass; database-local effect+claim/structural-ack+checkpoint commit atomically; external peer actions use AshOnetime three-state recovery and peer operation-key idempotency, with checkpoint only after finalized/replayed success; replay, digest mismatch, key rotation, crash/ambiguity, retention, value-free errors, and recovery are live-tested; nonce is not used for WAL retries | B2, B5, B6, B7 | New ADR required in this slice: logical-message effects and AshOnetime use |
| C2 | **Implement atomic sink-owned batch delivery** — expose `batch_delivery` and implement `handle_batch/1` without materializing lazy spilled transaction streams or weakening per-transaction order. slug:c2-batch-delivery | Batch applies ascending transactions and all messages in one destination transaction; skips only frontiers at/below checkpoint; persists highest LSN after all effects; mixed SCD1/SCD2/tenant/append/message/spill cases pass; mid-batch raise/write fault/crash yields zero net partial effects and zero checkpoint advance; telemetry counts one pass | B2, B6, C1 | Effect-once ADR from B2 plus Replicant batch contract |
| C3 | **Implement resumable incremental snapshots** — expose `snapshot_progress/0`, persist progress with each chunk, and use declared target origin/generation attributes through host Ash actions; append-log uses immutable generation identity. slug:c3-incremental-snapshots | v1 and incremental modes both satisfy fetched Replicant callback docs; first-table retry removes/closes only the retried snapshot generation; stream writes transition the logical row/version to stream origin and survive retry; SCD2 stores per-version provenance; unsupported nondeterministic/PK-less target shapes fail preflight; progress resumes atomically; completion is durable/idempotent; append-only observers prove zero physical repeats for both v1 and incremental restart; keyed, empty, tenant, SCD1, SCD2, append, crash, retry, batch, and backpressure marquees pass | B2, B3, B4, C2 | New ADR required in this slice: snapshot provenance and progress |
| C4 | **Implement Ash-native append-log mode** — make sink kind configurable and append immutable, classified, tenant-safe change records through host Ash actions with stable source ordering/identity. slug:c4-append-log | `:append_log` passes Replicant start guards; insert/update/delete/truncate/message metadata has a declared value-free/mapped shape; append identity prevents replay duplicates without overwriting distinct same-transaction effects; batch/snapshot applicability is explicit and tested; sensitive fields use B5 mechanisms; docs include full host resource contract | B2, B5, B6, C1, C2 | New ADR required in this slice: append-log data model |
| C5 | **Make schema and destination invariants continuously fail closed** — validate source-contract transitions, target migrations/indexes, SCD2 open-version constraints, replica identity, and config drift at start and runtime. slug:c5-schema-invariants | Under the checkpoint lock, the stored canonical manifest classifies additive/ignore changes and atomically advances only mapping-compatible transitions; destructive/identity/type/mapping-incompatible changes halt; doctor/runtime detect missing target tables/indexes/provenance, wrong SCD2 partial unique identity, wrong RIF, changed publication/config, and stale migrations; every failure freezes the checkpoint and emits structural diagnostics | B3, B4, C3, C4 | New ADR required in this slice: schema-change and destination invariant policy |
| D1 | **Ship deterministic install and upgrade tooling** — generate checkpoint/progress/provenance state, AshOnetime migrations, indexes, config, and upgrade steps through supported Mix/Igniter surfaces. slug:d1-install-upgrade | Fresh install and every published upgrade path generate idempotent migrations; `mix ecto.migrate` and rollback-supported steps pass on a clean host; ambiguous source/checkpoint data fails with an operator instruction; package includes templates/docs; migration drift gate is green; uninstall/destructive limits are explicit | A2, B2, C1, C3, C4, C5 | Migration ADRs from owning rows |
| D2 | **Provide read-only preflight and doctor surfaces** — inspect source identity/publication/RIF and destination Repo/resources/actions/tables/indexes/claims before delivery, with machine-readable and operator output. slug:d2-preflight-doctor | `AshReplicant.preflight/1` and `mix ash_replicant.doctor` return deterministic structural results; no row values/secrets appear; start runs required checks before cache/pipeline activation; permission/unreachable/timeout cases fail closed; every B1–C5 invariant has a named check and red fixture | B7, C5, D1 | Operationalization of the owning ADRs |
| D3 | **Publish observability and health contracts** — add typed telemetry builders, lag/frontier/snapshot/message/batch/preflight metrics, readiness semantics, and alert/runbook examples without label-cardinality or value leaks. slug:d3-observability | Event catalog is public and tested; metadata values are closed types; Telemetry.Metrics examples compile; readiness distinguishes healthy/catching-up/halted/misconfigured; alerts cover checkpoint conflict, sink halt, lag, snapshot stall, AshOnetime cleanup/reap/partition queues, and dependency/preflight failure; subscriber fault does not corrupt delivery | B5, C1, C2, C3, D2 | Value-free ADR from B5 plus operations ADR |
| D4 | **Prove crash, concurrency, and recovery behavior** — inject failures at every admission/effect/checkpoint/progress boundary and document deterministic operator recovery. slug:d4-fault-recovery | Live Postgres tests cover disconnects, process kills, DB rollback, pool exhaustion, lock timeout, duplicate start, stale source identity, partial batch/snapshot/message, AshOnetime ambiguity/recovery, schema halt, and restart; append-only ledgers prove zero net duplicate/loss where claimed; each halt maps to a tested runbook | C1, C2, C3, C4, D2, D3 | Effect-once and operations ADRs |
| D5 | **Set measured performance and resource bounds** — benchmark per-row, SCD2, batch, message/AshOnetime, v1/incremental snapshot, and append paths; expose safe tuning while preserving lazy single-pass streams. slug:d5-performance-bounds | Reproducible benchmark harness publishes throughput/latency/memory/DB-round-trip baselines and regression budgets; spilled transactions are enumerated once; batch materially amortizes commits; no unbounded key/content/response/telemetry cardinality; backpressure/timeout/pool guidance is tied to measurements; CI runs stable regression sentinels | C1, C2, C3, C4, D3 | New ADR required in this slice: performance and resource budgets |
| D6 | **Enforce the support matrix** — test every declared Elixir/OTP/PostgreSQL/Ash/AshPostgres/AshOnetime/Replicant combination and remove unsupported claims. slug:d6-support-matrix | CI uses Elixir 1.20.3/OTP 29 and PostgreSQL 15/16/17/18 with current minors; exact Ash 3.31.3 and floating latest Ash 3.x cells pass; the release lock records current Ash 3.31.3; minimum and latest dependency resolutions compile/test; logical-replication features run in each database cell or a mechanically cited upstream constraint narrows the matrix; README/CONTRIBUTING/package metadata match | A2, A3, A1, D4 | 1.0.0 support-contract ADR from A2 |
| D7 | **Complete adversarial conformance and security coverage** — enumerate every constructor/path for tenancy, actions, history, messages, snapshots, append, errors, telemetry, SQL, and config; mutation-prove the safety gates. slug:d7-conformance-security | Coverage matrix has no untested path; source values feed every parallel constructor; wrong tenant/source/Repo/action/index/identity cases fail before effects; property/byte/size/concurrency tests exercise both bounds; tests observe code under test; security scans, SBOM/license checks, dependency policies, and package secret checks pass | B3, B4, B5, B6, C1, C2, C3, C4, C5, D4 | All governing security/data ADRs |
| D8 | **Freeze and validate the public package contract** — specify every public module/function/type/DSL option/callback/error/default/deprecation; test the unpacked package in a fresh consumer. slug:d8-public-package | Public API inventory is fully documented/specced; removed test/deprecated surfaces have repo-wide consumer sweeps; SemVer/deprecation policy is published; Hex tarball contains all required code/templates/docs/licenses and no test/secrets/residue; clean consumer runs install, migration, compile-WAE, docs, and a default live CDC smoke using only the tarball | A4, D1, D6, D7 | New ADR required in this slice: public API and compatibility policy |
| D9 | **Ship complete operator and adopter documentation** — provide accurate quick start, resource/action recipes, security model, RIF/schema rules, AshOnetime operations, snapshots, messages, batching, append, upgrades, recovery, and runnable examples. slug:d9-operator-documentation | README, usage rules, Charter, AGENTS, operations guide, ADR index, CHANGELOG, Livebook, moduledocs, and package extras cover every user-reachable capability/default/failure; examples compile/run from the package; docs have no stale version/policy/support claims; lifecycle history is clearly frozen/superseded | A4, C1, C2, C3, C4, D2, D3, D5, D8 | Docs-authorship edge for all governing ADRs |
| E1 | **Bind release provenance and maintenance workflow** — make one auditable release candidate tie code, gates, docs, package, checksum, tag, GitHub release, Hex ownership/publish authorization, advisories, and rollback. slug:e1-release-provenance | Protected workflow/checklist builds once from the release commit; verifies clean tree/origin equality/version/changelog/source-ref/tag/package diff/checksum/SBOM/licenses/dependency audit/docs/consumer smoke; GitHub and Hex receipts are recorded; 2FA/scoped secret ownership and explicit publish authorization are documented; post-publish fetch/compile verifies immutable artifact | D6, D7, D8, D9 | New ADR required in this slice: release provenance |
| E2 | **Production release gate** — execute the complete evidence battery on the exact release candidate, close every roadmap dependency and review finding, then publish/tag the stable major release. slug:e2-production-release | Derived tracker reports every row RECORDED with no REOPENED/LEGACY/UNMATCHED blocker; local and CI full matrices pass; integration/performance/fault/security/package/default-path smokes pass on release bytes; independent T2 closeout and mandatory cross-vendor reviews are clean; CHANGELOG/version/tag/GitHub/Hex/docs/checksum receipts all bind one commit; no residual risk remains inside the production-ready claim | P0, A1, A2, A3, A4, B1, B2, B3, B4, B5, B6, B7, C1, C2, C3, C4, C5, D1, D2, D3, D4, D5, D6, D7, D8, D9, E1 | All tracked ADRs and the Forge completion contract |
